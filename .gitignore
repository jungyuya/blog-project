# Go Lang specific build artifacts
# Go 컴파일 결과물 및 임시 파일
*.exe
*.dll
*.so
*.dylib
*.o
*.a
*.obj
*.test
*.prof

# Go modules cache and vendor directory
# Go 모듈 캐시 및 벤더 디렉토리 (주로 CI/CD에서 사용)
/vendor/

# Serverless Framework specific artifacts
# Serverless Framework 배포 시 생성되는 임시 파일 및 디렉토리
.serverless/
.webpack/
.build/
.env # 환경 변수 파일 (민감 정보 포함 가능성)

# Go Lambda build artifacts (각 함수 디렉토리 내에 생성되는 실행 파일 및 ZIP 파일)
# Lambda 배포를 위해 수동으로 생성하는 바이너리 및 ZIP 파일
blog-backend/createPost/bootstrap
blog-backend/getPost/bootstrap
blog-backend/getPosts/bootstrap
blog-backend/updatePost/bootstrap
blog-backend/deletePost/bootstrap

blog-backend/createPost/*.zip
blog-backend/getPost/*.zip
blog-backend/getPosts/*.zip
blog-backend/updatePost/*.zip
blog-backend/deletePost/*.zip

# Terraform specific files
# Terraform 관련 임시 파일, 상태 파일, 프로바이더 바이너리
infrastructure/.terraform/ # Terraform 작업 디렉토리 (프로바이더 바이너리 포함)
infrastructure/*.tfstate   # Terraform 상태 파일
infrastructure/*.tfstate.backup # Terraform 상태 백업 파일
# infrastructure/.terraform.lock.hcl # Provider 버전 고정을 위해 이 파일은 보통 커밋합니다.

# Node.js / Next.js specific files
# Node.js 의존성 및 Next.js 빌드 결과물
node_modules/
.next/           # Next.js 빌드 아웃풋 디렉토리
out/             # Next.js 정적 익스포트 아웃풋 디렉토리
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE and OS specific files
# 개발 환경 및 운영체제 관련 임시 파일
.vscode/ # VS Code 설정 (개인 선호에 따라 제외)
.idea/   # IntelliJ / GoLand 설정
*.swp    # Vim swap files
*.bak    # 백업 파일
*.log    # 로그 파일 (deploy_debug.log 등)
.DS_Store # macOS specific
Thumbs.db # Windows specific